{
  "name": "pen",
  "version": "1.0.0",
  "author": "OpenBlock",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/openblock-plugin/pen.git"
  },
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "dev": "npx openblock-registry-cli dev",
    "build": "npx openblock-registry-cli build",
    "build:obfuscate": "npx openblock-registry-cli build --obfuscate",
    "publish": "npx openblock-registry-cli publish",
    "publish:dry": "npx openblock-registry-cli publish --dry-run",
    "validate": "npx openblock-registry-cli validate",
    "i18n:update": "npx openblock-registry-cli i18n update",
    "lint": "eslint . --ext .js --fix"
  },
  "openblock": {
    "name": {
      "formatMessage": {
        "id": "pen.name",
        "default": "Pen"
      }
    },
    "extensionId": "pen",
    "arch": [
      "*"
    ],
    "iconURL": "./assets/pen.png",
    "description": {
      "formatMessage": {
        "id": "pen.description",
        "default": "Draw with your sprites."
      }
    },
    "internetConnectionRequired": false,
    "main": "./src/pen.js",
    "translations": "./src/translations.js",
    "tags": [
      "scratch"
    ],
    "helpLink": "https://wiki.openblock.cc"
  },
  "devDependencies": {
    "eslint": "^5.3.0",
    "eslint-config-scratch": "^5.0.0",
    "husky": "^9.0.0"
  }
}
